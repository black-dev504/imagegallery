
<div class="max-lg mx-auto flex justify-center">
    <img
        src="{{ asset('storage/' . $image->img_path) }}"
        alt="{{ $image->name }}"
        class="w-full h-full object-cover"
    >
</div>
